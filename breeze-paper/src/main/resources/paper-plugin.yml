name: BreezePaper
version: '${version}'
main: me.bottdev.breezepaper.BreezePaper
api-version: '1.21'

dependencies:
  server:
    ASPaperPlugin:
      join-classpath: true
      required: false
      load: BEFORE
    InvUIWrapper:
      join-classpath: true
      required: false
      load: BEFORE
    LightGear:
      join-classpath: true
      required: false
      load: BEFORE
    LightAPI:
      join-classpath: true
      required: false
      load: BEFORE